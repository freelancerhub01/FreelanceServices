---
import ServiceLayout from '../../layouts/ServiceLayout.astro';
import { services } from '../../data/services';

const service = services.find((s) => s.slug === 'backend-schedulers')!;
---
<ServiceLayout service={service} />
